# torch-interaction-framework
This repository covers the implementation of an interaction framework built for a survival game in Unreal Engine 5. The framework is a two-component architecture built on a C++ interface. Any actor in the world can become interactable by simply adding a component and binding a delegate, with zero changes required to the player character or HUD.
